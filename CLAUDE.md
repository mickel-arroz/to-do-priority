@AGENTS.md

## Agent skills

### Issue tracker

Los issues viven en GitHub Issues de `mickel-arroz/to-do-priority`, gestionados con el CLI `gh`. See `docs/agents/issue-tracker.md`.

### Triage labels

Vocabulario canónico sin cambios: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: un `CONTEXT.md` en la raíz + `docs/adr/`. See `docs/agents/domain.md`.
